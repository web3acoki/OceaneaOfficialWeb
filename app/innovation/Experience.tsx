/**
 * Temporarily hidden. Keep this section component mounted so the dive experience
 * block can be restored later without changing the page structure.
 */
export default function Experience() {
  return null;
}
